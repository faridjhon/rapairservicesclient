import React from 'react';
import Header from '../Header/Header';

const Home = () => {
    return (
        <div class="container-fluid">
            <Header></Header>
        </div>
    );
};

export default Home;